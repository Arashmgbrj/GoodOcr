# GoodOCR - سرویس OCR آنلاین

## 🚀 معرفی
**GoodOCR** یک سرویس آنلاین قدرتمند برای تشخیص متن از تصاویر (OCR) است که با استفاده از فناوری‌های مدرن و الگوریتم‌های پیشرفته، متون فارسی و انگلیسی را با دقت بالا از تصاویر استخراج می‌کند.

🔗 **وبسایت**: [https://ocrgood.ir/](https://ocrgood.ir/)

## ✨ ویژگی‌های کلیدی
- ✅ پشتیبانی از متون **فارسی و انگلیسی**
- ✅ دقت بالا در تشخیص متون
- ✅ رابط کاربری ساده و کاربرپسند
- ✅ پردازش سریع و آنی تصاویر
- ✅ قابلیت آپلود فایل‌های مختلف تصویری
- ✅ خروجی متنی قابل کپی و ذخیره‌سازی

## 🛠️ فناوری‌های استفاده شده

### Frontend
- **Angular** - فریمورک پیشرفته Frontend
- **TypeScript** - توسعه نوع‌امن
- **Bootstrap** - استایل‌دهی و ریسپانسیو
- **HTML5/CSS3** - ساختار و ظاهر

### Backend
- **Flask** - فریمورک سبک و قدرتمند پایتون
- **Python** - زبان برنامه‌نویسی سمت سرور
- **Tesseract OCR** - موتور OCR اصلی
- **OpenCV** - پردازش تصویر
- **Pillow** - کار با تصاویر

### زیرساخت
- **Nginx** - وب سرور
- **Gunicorn** - WSGI Server
- **Virtualenv** - محیط مجازی پایتون

## 📦 نحوه نصب و راه‌اندازی

### پیش‌نیازها
- Python 3.8+
- Node.js 14+
- Angular CLI
- Tesseract OCR
- Virtualenv

### نصب Backend
```bash
# کلون کردن ریپازیتوری
git clone https://github.com/Arashmgbrj/GoodOcr.git
cd GoodOcr/backend

# ایجاد محیط مجازی
python -m venv venv
source venv/bin/activate  # برای Windows: venv\Scripts\activate

# نصب dependencies
pip install -r requirements.txt

# نصب Tesseract OCR
# برای Ubuntu/Debian:
sudo apt-get install tesseract-ocr tesseract-ocr-fas

# برای macOS:
brew install tesseract

# اجرای سرور
python app.py
```

### نصب Frontend
```bash
cd ../frontend

# نصب dependencies
npm install

# ساخت پروژه
ng build --prod

# اجرای سرور توسعه
ng serve
```

## 🏗️ معماری پروژه
```
GoodOCR/
├── backend/
│   ├── app.py              # فایل اصلی Flask
│   ├── requirements.txt    # کتابخانه‌های پایتون
│   ├── ocr_processor.py   # پردازشگر OCR
│   └── uploads/           # پوشه ذخیره فایل‌ها
└── frontend/
    ├── src/
    │   ├── app/           # کامپوننت‌های Angular
    │   ├── assets/        # فایل‌های استاتیک
    │   └── environments/  # تنظیمات محیطی
    └── angular.json       # تنظیمات Angular
```

## 🔧 API Endpoints
- `POST /upload` - آپلود تصویر و دریافت متن
- `GET /health` - بررسی وضعیت سرویس
- `POST /batch-upload` - پردازش گروهی تصاویر

## 🚀 استقرار (Deployment)

### با استفاده از Gunicorn و Nginx
```bash
# نصب Gunicorn
pip install gunicorn

# اجرای با Gunicorn
gunicorn -w 4 -b 0.0.0.0:5000 app:app

# پیکربندی Nginx
sudo nano /etc/nginx/sites-available/ocrgood
```

### نمونه کانفیگ Nginx
```nginx
server {
    listen 80;
    server_name ocrgood.ir www.ocrgood.ir;
    
    location / {
        proxy_pass http://localhost:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    location /static {
        alias /path/to/static/files;
    }
}
```

## 📝 نحوه استفاده
1. به وبسایت [https://ocrgood.ir/](https://ocrgood.ir/) مراجعه کنید
2. تصویر مورد نظر را آپلود کنید
3. منتظر پردازش OCR بمانید
4. متن استخراج شده را کپی یا ذخیره کنید

## 🤝 مشارکت در توسعه
ما از مشارکت‌های شما استقبال می‌کنیم! برای مشارکت:

1. ریپازیتوری را Fork کنید
2. یک Branch جدید ایجاد کنید (`git checkout -b feature/amazing-feature`)
3. تغییرات را Commit کنید (`git commit -m 'Add amazing feature'`)
4. به Branch اصلی Push کنید (`git push origin feature/amazing-feature`)
5. یک Pull Request ایجاد کنید

##
## 👥 توسعه‌دهندگان
- **آرش معظمی** - توسعه‌دهنده اصلی

## 🙏 تقدیر و تشکر
- از Tesseract OCR برای موتور قدرتمند OCR
- از جامعه متن‌باز برای ابزارها و کتابخانه‌های ارزشمند

---

⭐ اگر این پروژه برای شما مفید بود، لطفاً ستاره بدهید!
